# go to dfwweb named folder ->cd dfwweb

# install packages by running command -> npm i

# create a .env file and then add these keys in .env file

VITE_BASE_URL="Backend url(like http://localhost:4444)"
VITE_GOOGLE_MAPS_API_KEY = "YOUR_GOOGLE_PLACES_KEY"
VITE_STRIPE_PUBLISHABLE_KEY="YOUR_STRIPE_PUBLISH_KEY"

# now run this by running command -> npm run dev
